Command-line模式的使用介紹:
    本通訊模式為與藍牙裝置約定了某個結束符號，來表示發送與接收的結束（例如:\r\n），在發送時末尾會自動加入這個終止符號來標示數據發送結束，並等待主機的回覆。
    在收主機的回覆時，主機的回覆內容末尾也必須要有這個結束符號，否則接收線程會將內容會一直存放於緩存區中，不會顯示在螢幕上。
    （切記：主機的回覆訊息末尾要有約定的終止符號）。